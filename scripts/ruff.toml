[lint]
select = [
    # Pyflakes
    "F",
    # pycodestyle
    "E",
    "W",
]
ignore = [
    # Line too long
    "E501",
]

# Ignore `F401` (import violations) in all `__init__.py` files
[lint.per-file-ignores]
"__init__.py" = ["F401"]


